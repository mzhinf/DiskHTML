# DiskHTML 目录树视觉重构计划

## 目标

停止对现有 `details/summary` 样式做局部修补，重新建立稳定、紧凑、接近 Windows 文件浏览器的目录树组件，并通过真实 HTML 截图验收。

## 设计约束

- 保留现有快照数据结构、目录选择和右侧文件列表逻辑。
- 目录树每层缩进、展开按钮、连接线和文件夹图标必须使用同一套像素网格。
- 展开按钮只在存在子目录时显示；叶子目录保留等宽占位，不显示空方框。
- 当前目录以整行浅蓝色高亮；悬停和焦点状态清晰但不过度装饰。
- 不使用系统 Emoji，不依赖外部图片或 CDN。

## 执行阶段

- [x] 对照参考图记录当前页面与目标页面的视觉差异。
- [x] 将目录树从原生 `details/summary` 改为显式树节点结构。
- [x] 生成真实快照 HTML，并通过浏览器截图检查层级、对齐和状态。
- [x] 完成自动化回归、重建 EXE 并提交。

## 错误记录

- Python 模块 `diskhtml.exe_cli` 只定义入口，使用 `python -m` 不会执行命令；真实样本改由已构建的 `DiskHTML.exe` 生成。
- 计划记录的编码工具不可用，已改为内置的 UTF-8 编码实现。
