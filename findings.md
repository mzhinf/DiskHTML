# 本次任务发现

## 用户目标

- 将 HTML 冷备与 HTML 比较集成到 DiskHTML.exe。
- 给出当前 DiskHTML.exe 的使用指南。
- 移除可执行文件中不面向默认 HTML 工作流的无用界面功能。

## 当前发现

- 上一轮已在 Python 核心中实现 backup 和 compare-html 命令，以及单文件可视化 HTML。
- 当前 GUI 仍以 SQLite 项目为中心，工具栏包含新建项目、打开项目、扫描任务、恢复任务与旧式比较等入口。
- Windows 打包脚本和 build\dist\DiskHTML\DiskHTML.exe 已存在；本次应将 GUI 改为默认 HTML 工作流后重新构建并验证。

## 约束与决定

| 决定 | 原因 |
|---|---|
| 默认 GUI 仅保留冷备、HTML 比较、打开报告与扫描配置 | 与用户的 HTML 交付模型一致，避免 SQLite 项目功能干扰普通使用。 |
| 扫描、比较继续通过后台线程运行 | 防止 GUI 主线程被大目录扫描阻塞。 |
| SQLite 高级能力不删除核心代码 | 它仍可作为内部实现和兼容 CLI 的高级能力，但不再打入 GUI 主操作路径。 |
## 已知问题

| 问题 | 处理 |
|---|---|
| 任务计划首次被 URL 转义写入 | 已移除转义文本，改用 UTF-8 直接写入。 |
## 需求变更

- 比较工作流改为：从 HTML 冷备文件树选择一个历史目录，与本机任意目录比较；不再在 EXE 中比较两份 HTML。
- 浏览器本地页面不能直接请求任意目录访问权限，因此目录树选择和本机路径选择由 DiskHTML.exe 负责。

## 2026-07-25：层级 HTML 文件树设计调整

- 用户将项目本质明确为“包含每个文件 SHA-256 的文件树，并支持文件夹信息对比”。
- 参考 Snap2HTML 的公开说明后，采用其关键交互原则：单文件离线 HTML、可展开目录树、在树上定位目录并查看同目录文件。
- 冷备 HTML 不再只显示扁平表格：左侧渲染可展开层级文件树，单击目录会筛选右侧清单；清单明确显示 SHA-256、Hash 状态、大小和错误。
- 比较继续保持“从历史 HTML 选择目录，与任意本机目录比较”，不将两个 HTML 的比较作为默认操作。

## 发布验证结果

- 新构建的 DiskHTML.exe 已实际生成层级 HTML 冷备，并验证子目录对本机目录的 SHA-256 比较结果。
