# 变更日志

本项目按“未发布”区域持续记录面向用户和发布流程的变化；创建版本标签时再移动到对应版本章节。

## 1.0.0

### Added

- 无 PowerShell 依赖的 Python Windows 构建入口。
- 独立解压发布 ZIP、运行 EXE 并生成中文 HTML/SQLite 的冒烟验证。
- GitHub Windows CI、文档索引、架构表、贡献指南和安全策略。
- 所有自有代码文件必须包含用途说明的自动化检查。
- 根据最终发布目录自动生成 `LICENSE.txt`、英文第三方声明和逐组件完整许可证，并阻断未知组件或 Qt 残留。

### Changed

- 新增英文 README（README.en.md）及 README 语言链接；桌面状态栏新增中英文切换，切换时保留表单路径与选项。
- 离线快照与比较 HTML 新增浏览器端中文/英文切换，覆盖页面标题、搜索、表头、详情、状态、盘符提示和 CSV 导出列名；不改变嵌入数据格式。
- 产品版本统一以 `pyproject.toml` 为唯一来源；EXE 属性、桌面窗口和新生成 HTML 均显示 `v1.0.0`。

- 桌面生成界面从 PySide6 切换为 Python 标准库 `tkinter/ttk`；保留三个任务、后台扫描、暂停/继续/取消和中英文切换，不再分发 Qt 运行时。
- 桌面生成界面改用内置 Lucide SVG：应用使用目录树图标，三个任务页分别使用目录、比对和数据库图标；增加任务页底部安全边距，避免固定窗口内的选项被遮挡。
- 修复页签内容面板的负向上偏移，避免内容面板覆盖页签下边缘。
- 明确发布形态为完整 `onedir` 目录包加 ZIP，不能单独复制 EXE。
- PowerShell 构建脚本降为可选薄包装。
- README、用户指南、EXE 指南、CLI 指南和发布清单统一为当前三个任务页。

### Fixed

- 修复 CLI 指南中被写成字面量的换行与反斜杠。
- 移除代码说明中的“冷备”和无意义 `Helper` 占位术语。

## 0.1.0

首个待发布版本；创建正式标签前仍以 `Unreleased` 为准。
