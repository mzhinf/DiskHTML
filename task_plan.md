# 文件 Hash 冷备份校验工具开发计划

## 目标

开发一个面向 Windows 10/11 的可靠冷备份完整性管理工具：能够以流式方式扫描单文件、目录或卷，持久化 SHA256 索引，生成可离线浏览的报告，并比较文件、目录或历史扫描快照；在 8 TB、约 300 万文件规模下支持暂停、异常恢复和断点续扫。

## 当前阶段

阶段 4：项目导出与离线浏览报告（待 `file://` 人工验收）。

## 交付策略

- 第一条主线先完成 SQLite、SHA256 扫描、离线报告和比较引擎。
- CLI 先于 GUI，用于尽早验证数据可靠性、恢复能力和大数据量性能。
- PyQt6 GUI 在核心功能和接口稳定后接入，不复制核心业务逻辑。
- 每个阶段都必须通过自动化测试与人工验收条件，才进入下一阶段。
- 所有脚本包含有效中文注释；文档、提示、错误信息和其他非代码内容使用中文。
- 若仓库已启用 Git，每个可独立验证的修改批次都提交一次。

## 阶段与检查点

### 阶段 0：方案评审与需求冻结

- [x] 梳理功能、可靠性和规模要求。
- [x] 给出模块边界、数据库初稿、数据流和实施顺序。
- [x] 确认技术栈为 Python 3.12、SQLite、PyQt6。
- [x] 确认首个可用版本的范围和不做事项。
- [x] 确认大规模离线报告的分片方案。
- [x] 确认 SHA512 的定位：SHA256 永远必算，SHA512 为可选附加摘要。
- **状态：** 已完成
- **评审结论：** 用户于 2026-07-24 批准按本计划执行。

### 阶段 1：工程骨架与契约

- [x] 初始化 Git、Python 项目、`src` 布局、测试和质量检查配置。
- [x] 建立统一领域模型：扫描任务、卷信息、文件记录、比较结果、错误记录。
- [x] 定义扫描状态机、暂停/继续/取消语义和错误分类。
- [x] 定义配置格式、日志格式、时间格式、路径比较策略和版本兼容策略。
- [x] 编写架构决策记录与数据格式说明。
- **状态：** 已完成
- **验收：** 空项目可安装；CLI 帮助可运行；测试、静态检查和数据库迁移框架通过。

### 阶段 2：SQLite 持久化与迁移

- [x] 实现数据库连接、建表、迁移和事务边界。
- [x] 实现卷、扫描任务、目录、文件、错误和比较任务的数据访问层。
- [x] 为断点续扫、路径查找和比较查询建立索引。
- [x] 使用单写线程、批量事务和有界队列，避免并发写入争用。
- [x] 支持导入已有项目数据库并校验模式版本。
- **状态：** 已完成
- **验收：** 百万级模拟记录可流式写入和查询；中断事务不损坏数据库；旧模式可迁移或给出明确拒绝原因。

### 阶段 3：扫描、Hash 与断点恢复

- [x] 实现单文件、目录和卷根目录枚举，不依赖 Windows 文件 ID。
- [x] 实现 SHA256 分块读取；可选附加 SHA512，但不得替代 SHA256。
- [x] 使用“枚举生产者 + 有界任务队列 + Hash 工作线程 + 单数据库写线程”结构。
- [x] 实现排除目录、排除扩展名、长路径、权限错误和消失文件处理。
- [x] Hash 前后各读取一次元数据；文件在扫描中变化时重试，仍不稳定则记录状态而非输出可信 Hash。
- [x] 实现暂停、继续、取消、进程崩溃后按文件粒度恢复；被中断的单个文件从头计算。
- [x] 实现文件数、字节数、当前文件、速率、预计剩余时间等进度事件。
- [x] 采集卷标、文件系统、容量、分区，以及可获得的物理磁盘型号和序列号；采集失败时保留明确的缺失原因。
- **状态：** 已完成
- **验收：** 相同输入重复扫描得到相同摘要；暂停与重启后无漏扫/重复有效记录；变化中的文件不会被误标为可信；错误文件可追踪。

### 阶段 4：项目导出与离线浏览报告

- [x] 固化项目目录格式和格式版本。
- [x] 从 SQLite 流式导出 `disk_info.json`、`file_list.csv`、`hash_list.csv`、`summary.json`。
- [x] 生成 `report.html` 首页、目录树、文件详情、搜索/筛选和统计信息。
- [x] 对超大结果使用离线分片资源并按目录懒加载，避免把数百万文件一次性嵌入 HTML 或载入浏览器内存。
- [x] 所有 CSS、JavaScript 和数据资源写入项目目录，不使用 CDN 或联网依赖。
- [x] 导出到临时位置并原子完成，避免留下看似完整的半成品报告。
- **状态：** 进行中（待 `file://` 人工验收）
- **验收：** 断网且通过 `file://` 直接打开；中文和特殊字符正确；大数据报告首屏无需加载全量文件；CSV/JSON 与数据库统计一致。

### 阶段 5：比较引擎与比较报告

- [x] 实现文件对文件 SHA256 比较。
- [x] 实现实时目录、历史扫描快照及两者组合的比较入口。
- [x] 以规范化相对路径对齐记录，再比较大小和 SHA256；SHA256 是内容一致性的最终依据。
- [x] 使用 SQLite 排序/连接或流式归并，禁止把两侧全量记录读入内存。
- [x] 输出 `MATCH`、`CHANGED`、`ADDED`、`MISSING`、`ERROR`，明确 old/new 方向。
- [x] 生成可离线打开、可筛选并可导出 CSV 的 `compare_report.html`。
- [x] 对缺失 Hash、不稳定文件和读取失败单独标记，禁止将其判为一致。
- **状态：** 进行中（待本机 `file://` 人工验收）
- **验收：** 人工构造的新增、删除、修改、重命名和错误案例全部分类正确；统计等于明细；同内容不同路径按路径规则显示为缺失加新增。

### 阶段 6：CLI 首个可用版本

- [x] 提供 `scan`、`resume`、`status`、`export`、`compare`、`verify`、`import` 命令。
- [x] 支持命令行线程数、读取块大小和可选 SHA512；排除规则由配置文件固化为扫描快照。
- [x] 提供稳定退出码、结构化日志和可理解的中文错误提示。
- [x] 编写从扫描到多年后复验的操作文档。
- **状态：** 进行中（待本机 `file://` 人工验收）
- **验收：** 不启动 GUI 即可完成扫描、恢复、导出、比较和复验闭环。

### 阶段 7：PyQt6 GUI

- [x] 实现新建/打开项目、目标选择、排除规则和扫描配置。
- [x] 实现实时进度、暂停、继续、取消、错误列表和任务恢复提示。
- [ ] 实现报告打开、文件/目录比较和比较结果筛选。
- [ ] 将耗时任务放入后台线程，通过已有服务接口调用核心逻辑。
- **状态：** 进行中
- **验收：** GUI 全流程不阻塞主线程；关闭后可恢复未完成任务；与 CLI 输出一致。

### 阶段 8：可靠性、性能与发布

- [ ] 建立小样本、海量元数据、超大文件、权限错误、路径边界和中断恢复测试集。
- [ ] 在 HDD 与 SSD 上分别测量 Hash 吞吐、内存、数据库体积和报告生成时间。
- [ ] 根据介质类型给出保守默认并发数，允许用户调节；避免高并发导致 HDD 随机寻道退化。
- [ ] 做数据库完整性检查、导出一致性检查和项目自校验。
- [ ] 使用 PyInstaller 或等价方式打包，验证干净 Windows 10/11 环境。
- [ ] 完成用户手册、数据格式版本说明、升级/恢复说明和发布清单。
- **状态：** 待开始
- **验收：** 300 万文件压力模型下内存保持有界；异常重启可恢复；发布包可完成端到端冷备份校验。

## 建议项目结构

```text
DiskHTML/
├── pyproject.toml
├── README.md
├── docs/
│   ├── architecture.md
│   ├── data-format.md
│   └── recovery.md
├── src/
│   └── diskhtml/
│       ├── application/       # 用例编排与服务接口
│       ├── domain/            # 领域模型、枚举和规则
│       ├── scanner/           # 文件枚举、排除规则和元数据
│       ├── hashing/           # 分块 Hash 与工作线程
│       ├── database/          # SQLite、迁移和仓储
│       ├── disk/              # Windows 卷与物理磁盘信息
│       ├── compare/           # 文件和快照比较
│       ├── report/            # CSV、JSON、HTML 和资源分片
│       ├── cli/               # 命令行入口
│       └── ui/                # PyQt6 界面，最后接入
├── tests/
│   ├── unit/
│   ├── integration/
│   ├── fixtures/
│   └── performance/
└── scripts/                   # 开发和发布脚本，必须有中文注释
```

## 数据库模型初稿

### `schema_meta`

| 字段 | 说明 |
|---|---|
| `key` | 元数据键，如 `schema_version` |
| `value` | 元数据值 |

### `volumes`

| 字段 | 说明 |
|---|---|
| `id` | 主键 |
| `drive_letter` | 盘符，可空 |
| `volume_guid` | 卷 GUID，仅作信息记录，不参与文件一致性判断 |
| `volume_label` | 卷标 |
| `filesystem` | 文件系统 |
| `total_bytes`、`free_bytes` | 容量信息 |
| `disk_model`、`disk_serial` | 物理磁盘信息，可空 |
| `partition_json` | 分区关联信息 |
| `capture_error` | 采集不完整的原因 |

### `scan_jobs`

| 字段 | 说明 |
|---|---|
| `id` | UUID 主键 |
| `source_type`、`source_path` | 文件、目录或卷及原始路径 |
| `status` | `PENDING/SCANNING/PAUSED/COMPLETED/CANCELLED/FAILED` |
| `hash_algorithm` | 固定包含 `SHA256` |
| `options_json` | 排除规则、并发数、块大小等不可变扫描配置 |
| `started_at`、`updated_at`、`completed_at` | UTC ISO 8601 时间 |
| `files_seen`、`files_hashed`、`bytes_hashed` | 可恢复进度 |
| `format_version` | 项目数据格式版本 |

### `directories`

| 字段 | 说明 |
|---|---|
| `id`、`scan_id` | 主键与所属扫描 |
| `relative_path`、`path_key` | 原始相对路径与比较键 |
| `parent_path_key` | 树和统计查询使用 |
| `scan_status`、`error_message` | 枚举状态与错误 |

### `files`

| 字段 | 说明 |
|---|---|
| `id`、`scan_id` | 主键与所属扫描 |
| `relative_path`、`path_key` | 保真路径与规范化比较键 |
| `name`、`extension` | 文件名与扩展名 |
| `size_bytes` | 文件大小 |
| `created_time`、`modified_time` | UTC ISO 8601 时间 |
| `mtime_ns` | 变化检测使用的高精度时间 |
| `sha256` | 必需内容摘要，未成功时为空 |
| `sha512` | 可选附加摘要 |
| `hash_status` | `PENDING/HASHING/OK/UNSTABLE/ERROR` |
| `attempt_count`、`error_code`、`error_message` | 重试和审计信息 |
| `hashed_at` | Hash 完成时间 |

建议约束与索引：

- `UNIQUE(scan_id, path_key)`，发现路径比较键冲突时停止静默覆盖并记录错误。
- `INDEX(scan_id, hash_status)` 支持恢复任务。
- `INDEX(scan_id, path_key, size_bytes, sha256)` 支持比较。
- `INDEX(scan_id, parent_path_key)` 支持报告按目录流式导出。

### `compare_jobs` 与 `compare_entries`

| 表 | 关键字段 |
|---|---|
| `compare_jobs` | 左右来源、方向、状态、创建时间、各分类计数 |
| `compare_entries` | `relative_path`、`status`、old/new 大小、old/new SHA256、错误说明 |

## 核心数据流

```text
目标与配置
  → 文件枚举器
  → 有界 Hash 队列
  → 多个分块读取工作线程
  → 有界结果队列
  → 单 SQLite 写线程
  → 完成的扫描快照
  → 流式 CSV/JSON/HTML 导出或 SQL/归并比较
```

## 项目输出格式建议

```text
archive_report/
├── archive.sqlite3          # 权威索引与断点恢复数据
├── disk_info.json
├── file_list.csv
├── hash_list.csv
├── summary.json
├── report.html
└── report_assets/           # 超大报告的本地样式、脚本和分片数据
```

- SQLite 是权威数据源，CSV/JSON/HTML 是可重建导出物。
- `report.html` 保持入口不变；`report_assets` 使用可由 `file://` 加载的本地 JavaScript 分片，不依赖本地服务器。
- CSV 使用 UTF-8 with BOM，时间使用带 `Z` 的 UTC ISO 8601；HTML 可显示本地时区。
- 项目根目录记录数据格式版本和生成器版本，便于多年后兼容读取。

## 关键正确性规则

1. SHA256 永远计算，其他算法只能作为附加信息。
2. 不以大小、时间、路径、Windows 文件 ID 代替内容 Hash。
3. 比较先用相对路径建立对应关系，再用大小快速识别差异，最终由 SHA256 判定内容一致。
4. Hash 前后元数据不一致的文件不得标为成功。
5. 权限错误、读取错误、链接循环和不稳定文件必须进入报告，不得静默跳过。
6. 默认不跟随目录符号链接和 Windows 重解析点，避免循环和越界；可在后续提供显式开关。
7. 断点恢复以“已完整提交的文件”为边界；不尝试序列化 Hash 内部状态。
8. 所有导出统计从同一已完成快照生成，防止数据库继续变化造成报告内部不一致。

## 测试与验收矩阵

| 范畴 | 代表场景 |
|---|---|
| Hash 正确性 | 空文件、已知向量、超大文件、扫描中被修改 |
| 路径 | 中文、Emoji、超长路径、大小写、同名不同层级、非法/冲突比较键 |
| 枚举 | 空目录、权限拒绝、重解析点、文件扫描中消失 |
| 恢复 | 暂停、取消、强制终止、数据库事务中断、重启继续 |
| 比较 | 完全一致、内容变更、新增、缺失、重命名、Hash 缺失 |
| 报告 | 离线打开、特殊字符转义、分片加载、统计与数据库一致 |
| 规模 | 300 万模拟记录、少量超大文件、HDD/SSD 不同并发度 |
| 兼容 | 数据库模式升级、旧项目只读导入、未知新版本拒绝写入 |

## 需要用户评审的选择

1. 首个可用版本是否接受“CLI 完整闭环、GUI 后置”的实施顺序。
2. 是否确认 SQLite 文件属于项目目录的长期权威记录；CSV/JSON/HTML 均可重新生成。
3. 是否接受超大报告增加 `report_assets` 子目录，以保证离线浏览且不一次加载数百万条记录。
4. 是否确认 SHA512 为可选附加摘要，不能关闭 SHA256。
5. 默认是否“不跟随符号链接、目录联接和其他重解析点”。
6. 首版是否只支持本机路径与本地卷，暂不承诺 SMB/NAS 的完整 Windows 语义。
7. 是否需要在首版支持“重新命名检测”；建议首版按要求输出 `MISSING + ADDED`，不猜测重命名。

## 风险与缓解

| 风险 | 缓解措施 |
|---|---|
| 多线程在机械硬盘上引起随机寻道，反而变慢 | 默认保守工作线程数，提供基准测试与可调参数 |
| 扫描期间文件变化导致错误 Hash | Hash 前后元数据检查、有限重试、`UNSTABLE` 状态 |
| 单个 HTML 包含数百万记录而不可用 | 离线分片、目录级懒加载、服务端无依赖 |
| Windows 路径大小写与 Unicode 造成映射冲突 | 同时保存原始路径和明确版本化的 `path_key`，冲突不静默覆盖 |
| 磁盘型号和序列号权限/映射不稳定 | 信息采集与完整性判断解耦，记录来源及错误 |
| CSV 不足以表达错误与恢复状态 | SQLite 为权威数据源，CSV 作为互操作导出 |
| 数年后软件或模式升级无法读取旧项目 | 模式版本、只读兼容、迁移测试和格式文档 |

## 错误记录

| 错误 | 尝试 | 处理 |
|---|---:|---|
| 系统 `python` 命令无法启动 | 1 | 已固定使用 `@.venv` 的 Python 3.12.13，并成功运行会话恢复脚本 |
| 初次检查时 Git 仓库尚无提交 | 1 | 阶段 1 完成后按仓库纪律创建首个提交 |
| 计划文件写入受到 Windows 沙箱临时阻断 | 1 | 改用标准补丁程序，并持续执行差异与 UTF-8 校验 |
| Codex 内置补丁执行器被 Windows 拒绝访问 | 3 | 改用 Git 附带的标准补丁程序，并逐次执行 UTF-8 与差异检查 |
| `ensurepip` 和 `pip install` 在沙箱内无法写系统临时目录 | 2 | 经授权在沙箱外完成 `@.venv` 的 pip 引导和开发依赖安装 |
| 单元测试在沙箱内无法写测试临时目录 | 1 | 清理残留目录后经授权在沙箱外复跑，10 项测试全部通过 |
| 文档间距补丁因上下文行号变化失败 | 1 | 读取实际行号后改用完整文件补丁，清理 .orig 和 .rej |
| 浏览器自动化拒绝访问本地 `file://` URL | 1 | 不绕过浏览器安全策略；保留相对资源和无网络请求的自动化测试，改由本机人工验收直开行为 |
| PyQt6 下载在授权安装中超时 | 1 | 缓存中无可用构件；不以其他 GUI 框架替代，待网络或本地构件可用后继续第七阶段 |

## 变更纪律

- 方案已获批准；后续只按已记录的版本与兼容策略演进。
- 每完成一个阶段，更新本文件、`findings.md` 和 `progress.md`。
- 发现需求冲突或可靠性风险时先更新设计与测试，再实现。
- 不删除仍有效的旧格式说明；废弃内容要标记版本和迁移方式。
