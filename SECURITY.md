# 安全策略

## 支持版本

在首个正式 GitHub Release 之前，只维护默认分支。发布后，本表应改为明确的版本范围。

| 版本 | 安全更新 |
|---|---|
| 默认分支 | 支持 |
| 未标记的历史构建 | 不支持 |

## 报告漏洞

请不要在公开 Issue 中披露可利用细节。使用 GitHub 仓库的“Security → Report a vulnerability”私密报告功能，提供：

- 受影响版本或提交；
- 复现步骤和最小样本；
- 影响范围；
- 可行的缓解建议；
- 是否已公开披露。

维护者应先确认收到报告，再在修复和发布准备完成后协调公开说明。

## 特别关注范围

DiskHTML 会递归读取用户选择的目录、生成 `file:///` 链接、解析自有 HTML/SQLite 快照并解压发布 ZIP。安全修改应重点检查路径穿越、符号链接循环、HTML/JavaScript 转义、SQLite 输入校验和清理路径边界。