# 需求发现与设计依据

## 已确认需求

- 平台为 Windows 10/11。
- 输入支持单文件、文件夹和整个磁盘，目录递归扫描。
- 文件记录至少包含路径、文件名、扩展名、大小、创建时间、修改时间和 SHA256。
- 扫描时采集磁盘名称、型号、序列号、容量、文件系统、分区和扫描时间。
- 生成 SQLite 长期索引，以及 JSON、CSV 和可离线打开的 HTML 报告。
- 比较支持文件对文件、目录对目录和两次扫描结果。
- 目录比较按相对路径对齐，输出相同、修改、新增和缺失。
- 面向 8 TB、约 300 万文件，内存必须有界。
- Hash 需要多线程、进度、暂停和断点继续。
- 支持排除目录、排除扩展名、导入历史扫描、导出 CSV。
- 可靠性优先于 UI 复杂度，UI 最后实现。
- SHA256 是必须算法；不依赖 Windows 文件 ID；内容 Hash 是一致性判断依据。

## 需求冲突与解释

| 项目 | 原始要求 | 计划解释 |
|---|---|---|
| Hash 算法 | “Hash 必须使用 SHA256”与“SHA256/SHA512 切换”并存 | SHA256 永远必算；SHA512 可作为附加摘要，不能替换或关闭 SHA256 |
| 输出目录 | 示例未列 SQLite，但性能要求指定数据库缓存 | 增加 `archive.sqlite3` 作为权威索引，原有 JSON/CSV/HTML 全部保留 |
| 内容唯一判断 | 目录比较又要求按相对路径、大小、Hash | 相对路径只建立两侧对应关系，大小用于快速差异检查，SHA256 最终判定对应文件内容 |
| HTML 单文件 | 要求静态离线报告，但未要求严格单文件 | `report.html` 为固定入口；大数据使用同目录本地资源分片，仍可离线打开 |

## 初步技术决策

| 决策 | 理由 |
|---|---|
| Python 3.12 + SQLite + PyQt6 | 符合推荐技术栈，开发效率高，核心可先做 CLI，GUI 后接入 |
| SQLite 是权威数据源 | 支持事务、恢复、索引和流式查询，比 CSV 更适合数百万文件 |
| 单写线程配合批量事务 | 避免 SQLite 多写者争用，同时保持 Hash 工作者并行 |
| 有界队列贯穿流水线 | 防止枚举速度高于 Hash/写入时占满内存 |
| 文件粒度断点恢复 | 标准 `hashlib` 状态不可可靠跨进程持久化；中断文件从头算更安全 |
| Hash 前后元数据复核 | 防止扫描中被修改的文件产生看似可信但实际不对应稳定内容的摘要 |
| CLI 先于 GUI | 尽早验证数据模型、恢复和报告接口，避免 UI 驱动核心返工 |
| 大报告按目录分片 | 数百万条记录不适合全部嵌入单一 HTML 或一次性载入浏览器 |
| 默认不跟随重解析点 | 防止目录循环、重复扫描和扫描根目录之外的内容 |

## 关键实现发现

- 多线程 Hash 不一定让 HDD 更快；需要保守默认值和实际介质基准，而不是固定使用 CPU 核心数。
- 物理磁盘序列号与卷/分区不是一对一关系，且采集可能受权限、USB 桥接和驱动影响；该信息不能参与文件一致性判定。
- 扫描结果长期保存需要同时版本化数据库模式、路径规范化算法、导出格式和生成器版本。
- 报告导出必须针对一个冻结的已完成扫描快照，否则统计与文件明细可能来自不同状态。
- 文件读取错误和目录枚举错误属于报告内容的一部分；静默跳过会让“备份完整”结论失真。
- 相同内容移动到不同路径时，严格按需求应表现为旧路径 `MISSING`、新路径 `ADDED`；重命名推断可以后续作为辅助视图。
- `file_list.csv` 与 `hash_list.csv` 存在字段重叠，但仍按需求同时生成；两者都从 SQLite 流式导出。

## 已批准范围

- CLI 优先，GUI 后置。
- 项目目录允许增加 `archive.sqlite3` 与 `report_assets`。
- SHA512 仅为附加 Hash，SHA256 永远必算。
- 默认不跟随符号链接、目录联接和其他重解析点。
- 网络共享、重命名推断和严格单文件 HTML 不属于首版承诺。

## 当前仓库状态

- 用户已批准按计划执行，阶段 1、2、3 已通过验收，当前进入阶段 4。
- 项目统一使用 `@.venv`，Python 版本为 3.12.13；pip、可编辑安装和 Ruff 已就绪。
- `pyproject.toml` 已配置 setuptools 构建、`diskhtml` 命令入口和 Ruff/coverage 质量规则。
- Git 仓库已初始化；阶段 1 已作为首个可验证批次提交。
- 系统的 `python` 命令不可用，项目命令必须显式使用 `.venv\Scripts\python.exe`。

## 参考资源

- 用户提供的项目需求文本。
- `planning-with-files` 技能模板与工作流。

## 阶段 1 实施发现

- 统一领域契约已覆盖扫描任务、卷信息、文件记录、比较结果和错误记录。
- 扫描状态机允许暂停后继续，也允许取消或失败任务从已提交的文件边界恢复；完成状态不可逆。
- 配置采用版本化 TOML，日志支持中文文本与稳定字段的单行 JSON。
- CLI 当前只暴露 `init-db` 与 `check-db` 骨架命令，扫描和比较命令仍留在后续阶段实现。
- 现有数据库与扫描器代码属于阶段 2、3 的提前实现；本阶段仅接入契约并验证迁移骨架，不宣告后续阶段完成。
- 阶段 1 验收结果：可编辑安装成功，10 项单元测试、Ruff 静态检查、Ruff 格式检查、CLI 帮助和 SQLite 完整性检查全部通过。
## 阶段 2 实施发现

- 数据库模式从版本 1 升级为版本 2：显式迁移序列、`migration_history` 与未来版本拒绝策略已生效。
- `Database.batch()` 使用 `BEGIN IMMEDIATE`，异常时回滚整批文件、错误、进度和比较记录；扫描器每批完成的 Hash 结果使用一次事务提交。
- 仓储接口已覆盖卷、扫描、目录、文件、错误、比较任务和比较明细，并以迭代器提供流式查询。
- 版本 1 数据库可原地迁移；版本 999 数据库会被明确拒绝且连接会立即关闭。
- 规模验收：临时数据库单事务写入并流式计数 1,000,000 条记录，计数正确、`integrity_check = ok`，耗时 28.929 秒。
## 阶段 3 实施发现

- SQLite 连接允许后台扫描线程使用，但所有读写通过可重入锁与单个批量事务串行，避免跨线程异常和写入交错。
- 暂停在文件边界持久化为 `PAUSED`；取消后已提交文件保留，`CANCELLED` 与 `FAILED` 都可复用稳定结果恢复。
- 文件系统访问对 Windows 使用扩展路径前缀；符号链接和重解析点默认跳过并记录错误。
- Hash 前后元数据不一致会产生 `UNSTABLE`，不会保存 SHA256；读取失败和源路径不存在均会写入扫描错误。
- 卷、分区和物理磁盘信息通过 Windows API 与 PowerShell 采集；因真实硬件元数据属于敏感信息，运行验证采用模拟返回与失败降级测试。
- 阶段 3 验收结果：27 项单元测试、Ruff 静态检查和格式检查全部通过。

## 阶段 4 实施发现

- 导出只允许冻结的 `COMPLETED` 扫描，并在同级隐藏临时目录写完全部内容后原子改名发布。
- CSV 采用 UTF-8 BOM；JSON、HTML、样式、脚本和数据分片均为 UTF-8 本地文件。
- 分片清单只保存统计与顶级目录清单，文件明细以本地脚本按需加载，避免首屏载入全量记录。
- 每个已加载分片在浏览器内构建目录树，并支持路径、状态、错误文本筛选以及文件详情查看。
- 自动化浏览器策略拒绝访问 `file://` URL，故实现可由静态资源测试覆盖，直开行为仍需本机人工验收。

## 阶段 5 实施发现

- 历史快照比较使用两个按 `path_key` 排序的 SQLite 迭代器归并，内存仅保留当前两条记录和一个有界写入批次。
- 实时路径比较先通过既有扫描器固化当前快照，再复用同一比较核心，保证文件、目录和卷遵守既有 Hash 与错误审计规则。
- 比较报告按结果状态分片，CSV 以 UTF-8 BOM 写入，并使用临时目录加原子改名发布。
