# 进度记录

## 2026-07-25：DiskHTML.exe 的 HTML 工作流集成

### 阶段 1：审查打包与当前 GUI

- **状态：** 进行中
- 已完成：
  - 读取规划技能说明并检查既有任务计划。
  - 记录本次 EXE 集成、功能精简和使用指南目标。
- 待完成：
  - 审查打包脚本、当前 EXE 产物和 GUI 入口。
  - 设计并实现精简后的 HTML 工作流界面。
  - 构建 DiskHTML.exe、测试并更新指南。

## 测试结果

| 测试 | 结果 |
|---|---|
| 待执行 | 待执行 |

## 错误记录

| 时间 | 错误 | 处理 |
|---|---|---|
| 无 | 无 | 无 |
### 记录修复

- 首次追加任务计划时误写入 URL 转义文本；已在下一次操作中移除并改为直接 UTF-8 内容。
### 需求调整

- 用户明确比较目标为“HTML 文件树中的任意目录”与“本机任意目录”，已停止将两个 HTML 快照作为 EXE 默认比较入口的方案。
### 测试错误

- EXE 入口端到端测试首次运行因遗漏导入 TemporaryDirectory 失败；已补齐导入后重跑。- 补齐导入时误将 PowerShell 换行转义写为字面量，导致语法错误；已替换为真实换行。
### 阶段完成：设计与实现

- EXE 默认界面已收敛为 HTML 冷备和“HTML 树目录对本机目录”比较。
- 精简 EXE 命令入口支持 backup 和 compare-source；无参数仍启动 GUI。
- 完整回归 61 项通过；格式与静态检查通过。
### 构建错误

- 直接执行 build_windows.ps1 被本机 PowerShell 执行策略拦截；将改用仅针对该脚本进程的 ExecutionPolicy Bypass。- 新 EXE 为窗口子系统程序，PowerShell 未设置 LASTEXITCODE，导致首次命令验证脚本误判；后续改为检查实际 HTML 产物。- EXE compare-source 生成了报告，但中文输出文件名被错误解码为乱码；需修正打包入口的 Unicode 参数处理后重建。- 已复核比较报告以中文文件名正确落盘并含 MATCH 结果；上一次失败来自验证脚本变量命名，不是 EXE Unicode 参数问题。- 已在无需权限的操作中完成目录辅助函数修复；静态检查待重跑。
### 当前阻塞

- 受限沙箱无法写入测试临时目录；经授权运行的完整回归 61/61 已通过。
- 当前唯一阻塞是旧 DiskHTML.exe 占用发布目录；需要关闭后重新打包并提交。

### 需求调整：层级 HTML 文件树

- 用户更新目标为带 SHA-256 的层级文件树与文件夹信息对比。
- 已查阅 Snap2HTML 的公开项目说明：其交付物是单文件、类似应用的 HTML，含可导航树和文件搜索；DiskHTML 据此补齐生成 HTML 的树形可视化。
- 已完成 Python 源码静态检查与编译检查；完整单元测试、EXE 重建和 Git 提交仍受自动审批额度限制。

### 构建阻塞

- 完整回归 61/61 通过；Ruff 与格式检查通过。
- 重新打包时 PyInstaller 已完成分析、归档和 EXE 生成，但旧的 `build\dist\DiskHTML\DiskHTML.exe`（进程 6956）正在运行，锁住发布目录中的 PyQt DLL，无法原子替换发布包。
- 未强制结束用户进程；待用户关闭该窗口后重试构建、执行 EXE 端到端验证并提交。

### 发布验证完成

- 已关闭占用的旧 EXE 并重新构建 `build\dist\DiskHTML\DiskHTML.exe`。
- 真实 EXE 端到端验证在 `C:\tmp\diskhtml-e2e-4a972d95ff16480b8f4c917b85e06bad` 创建嵌套样本：冷备 HTML 含 `id="tree"`、`createTree()` 与 `SHA-256`；历史 `资料/照片` 与本机目录比较得到 MATCH、CHANGED、ADDED，且不包含树外文件。
- 实现、文档、完整回归（61/61）、静态检查和发布验证均已完成，待 Git 提交。

### 详情页与 SQLite 冷备索引

- 已增加目录创建/修改时间迁移，并在 HTML 快照中保留空目录和目录元数据。
- HTML 冷备与比较报告均使用左树右详情布局；关键回归和完整回归 61/61 通过，Ruff 与格式检查通过。
- 新版发布包尚未重建：旧 DiskHTML.exe 进程 20416 锁住发布目录；不强制结束用户进程。

### 发布验收：详情与 SQLite

- 已重建 DiskHTML.exe，并在 `C:\tmp\diskhtml-detail-e2e-ee10dfe6938341f68059559db3d30bcf` 完成端到端验证。
- `backup` 同时生成 HTML 和 SQLite；`render-sqlite` 成功生成新版 HTML；空目录、详情列、比较“是否相同”和红点逻辑均已验证。

## 2026-07-25??????????????

- ?? GUI ??????????????????????????????????????
- ????????? TOML???????????????????? UI??????/TOML ???
- ?? Windows junction?`Path.is_symlink()` ? False?? `os.lstat(...).st_file_attributes` ? 1040?????????????? junction ???????????????????
- ???? UI ????????????????????????????????????
## 本轮完成
- 移除已移除的报告查看与扫描配置入口。
- 比较报告复用快照布局，仅增加状态列。
- 修复 Windows junction 根目录软链接跟随。
- 已通过 61 项测试、 Ruff 检查与格式检查。

## 快照HTMLUI重构

- 已读取参考图并创建分阶计划。

## 快照 HTML UI 重构完成

- 完成 Snap2HTML 风格的单文件离线界面。
- 已通过真实快照生成、读取和全量测试验证；待重建 EXE。

## 报告差异与桌面生成界面重构

- 已完成代码审查：报告问题集中在 `archive_ui.py`，桌面交互集中在 `ui.py`。

- 报告脚本第一次精确文本替换未命中压缩的 `showDetail` 函数；已改为按函数边界替换。

- 已完成报告差异颜色、精确搜索与三页签桌面重构；61 项全量测试通过，EXE 已重建。

- 已将报告页面设为视口固定布局：目录树、文件列表独立滚动，详情区固定于右侧底部。
