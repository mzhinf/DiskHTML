# 开发进度记录

## 会话：2026-07-24

### 工程实施启动

- **状态：** 进行中
- 已完成：
  - 用户批准按计划执行，阶段 0 已冻结。
  - 使用项目 `.venv` 成功运行会话恢复脚本，确认 Python 为 3.12.13。
  - 确认 `pyproject.toml` 为待完善的最小配置。
- 下一步：
  - 初始化 Git，建立包结构、数据库迁移、CLI 入口与基础测试。

### 方案评审阶段

- **状态：** 评审中
- **开始时间：** 2026-07-24
- 已完成：
  - 阅读并采用 `planning-with-files` 技能流程。
  - 尝试恢复既有计划会话，发现系统 `python` 命令不可用。
  - 检查工作目录与 Git 状态，确认目录为空且尚未初始化 Git。
  - 整理功能需求、冲突项、可靠性约束和规模要求。
  - 设计八个实施阶段、模块结构、数据库模型、数据流和验收矩阵。
  - 明确核心先行、CLI 先于 GUI 的交付顺序。
  - 记录需要用户确认的七项设计选择。
- 本轮未执行：
  - 未初始化工程或 Git。
  - 未安装依赖。
  - 未编写或运行产品代码。
- 创建文件：
  - `task_plan.md`
  - `findings.md`
  - `progress.md`

## 测试结果

| 检查 | 预期 | 实际 | 状态 |
|---|---|---|---|
| 工作目录现状 | 确认是否存在需保护的文件 | 目录为空 | 通过 |
| Git 状态 | 确认能否按要求提交 | 当前不是 Git 仓库 | 已记录 |
| 会话恢复脚本 | 检查是否有未同步上下文 | `python` 无法启动，且无既有项目文件 | 已记录 |
| 计划完整性 | 覆盖结构、数据库、扫描、报告、比较、测试和风险 | 已写入 `task_plan.md` | 通过 |

## 错误日志

| 时间 | 错误 | 尝试 | 处理 |
|---|---|---:|---|
| 2026-07-24 | PowerShell 无法启动 `python.exe` | 1 | 未重复相同失败；记录环境问题，工程初始化时选择并固定解释器 |
| 2026-07-24 | `git status` 提示不是 Git 仓库 | 1 | 本轮不提交，计划获批后再初始化 |

## 五问恢复检查

| 问题 | 回答 |
|---|---|
| 现在在哪里？ | 阶段 4：项目导出与离线浏览报告 |
| 接下来去哪里？ | 固化项目导出格式、CSV/JSON、离线 HTML 报告与分片加载 |
| 最终目标是什么？ | 建成可长期保存、恢复和验证冷备份完整性的 Windows 工具 |
| 已经了解什么？ | 阶段 1、2、3 的契约和实施发现见 `findings.md` |
| 已经完成什么？ | 阶段 3 已验收，27 项测试与质量检查通过 |

### 阶段 1：工程骨架与契约
- **状态：** 已完成
- **完成时间：** 2026-07-24
- 已完成：
  - 在 `@.venv` 中引导 pip，并以可编辑模式安装项目和 Ruff。
  - 补齐版本化 TOML 配置、结构化日志、CLI 入口和质量检查配置。
  - 建立扫描任务、卷信息、文件记录、比较结果、错误记录及扫描状态机契约。
  - 编写 `README.md`、架构决策、数据格式与恢复语义中文文档。
  - 添加配置、CLI、数据库迁移和状态机测试。
- 验收结果：
  - `python -m unittest discover -s tests -v`：10 项通过。
  - `python -m ruff check .`：通过。
  - `python -m ruff format --check .`：通过。
  - `python -m diskhtml --help` 与已安装的 `diskhtml --version`：通过。
  - SQLite 建表、模式版本和 `integrity_check`：通过。
- 清理：已删除补丁工具备份和失败测试留下的四个临时目录。
- 下一步：进入阶段 2，审查并完善 SQLite 事务边界、仓储接口、批量写入和迁移兼容。

### 阶段 2：SQLite 持久化与迁移

- **状态：** 已完成
- **完成时间：** 2026-07-24
- 已完成：
  - 模式升级到版本 2，提供顺序迁移、历史记录、旧库升级和未来版本拒绝。
  - 实现 `Database.batch()` 原子批量写入、回滚与流式仓储查询。
  - 增加比较任务与明细仓储、扫描错误索引和数据库迁移说明。
  - 扫描器改为每批完成的 Hash 结果单事务提交。
- 验收结果：
  - 16 项单元测试通过，覆盖迁移、未来版本拒绝、回滚、10,000 条批量写入、比较仓储和扫描集成。
  - 1,000,000 条临时记录单事务写入与流式查询通过，`integrity_check = ok`，耗时 28.929 秒。
  - Ruff 静态检查与格式检查通过。
- 下一步：进入阶段 3，完成文件系统边界、暂停/取消状态持久化、变化文件处理和恢复细节。
### 阶段 3：扫描、Hash 与断点恢复

- **状态：** 已完成
- **完成时间：** 2026-07-24
- 已完成：
  - 实现单文件、目录和卷根目录扫描，使用有界 Hash 队列和单个数据库写入路径。
  - 实现 SHA256、可选 SHA512、分块读取、变化重试、`UNSTABLE` 与错误审计。
  - 实现暂停状态持久化、取消后的文件边界恢复、失败任务恢复和跨线程数据库保护。
  - 实现 Windows 长路径、重解析点跳过、排除规则、卷/分区/物理磁盘采集及失败降级。
  - 添加当前路径、吞吐率和基于已枚举字节的 ETA 进度事件。
- 验收结果：
  - 27 项单元测试通过，覆盖暂停、取消、失败恢复、变化文件、单文件、长路径、卷信息和扫描错误。
  - Ruff 静态检查与格式检查通过。
- 下一步：进入阶段 4，生成可离线浏览且适用于大数据量的导出报告。

### 阶段 4：项目导出与离线浏览报告

- **状态：** 进行中（待 `file://` 人工验收）
- 已完成：
  - 以临时目录和原子改名导出冻结扫描的 CSV、JSON、报告首页、本地资源和目录分片。
  - 提供本地目录树、当前分片搜索筛选和文件详情交互；不依赖 CDN、网络服务或 `fetch`。
  - 添加导出统计、特殊字符、分片、原子发布和本地资源约束的测试。
- 验证结果：
  - 29 项单元测试、Ruff 格式检查、Ruff 静态检查与 Git 差异检查通过。
  - 浏览器自动化拒绝访问 `file://` URL，未绕过该策略；本机人工直开验收仍待完成。
- 下一步：在 Windows 本机通过 `file://` 打开报告完成验收，然后进入阶段 5 的比较引擎。

### 阶段 5：比较引擎与比较报告

- **状态：** 进行中（待本机 `file://` 人工验收）
- 已完成：
  - 实现历史快照、实时路径对历史快照、两个实时路径三种比较入口。
  - 以流式路径归并和有界 SQLite 批量写入持久化 `MATCH`、`CHANGED`、`ADDED`、`MISSING`、`ERROR`。
  - 导出比较 CSV、统计 JSON、状态分片和可离线筛选的 `compare_report.html`。
- 验证结果：5 项比较测试通过，覆盖所有分类、非可信摘要、大小写路径归并、实时目录和离线导出。
- 下一步：运行全量回归并在本机人工验收两类 `file://` 报告，然后接入 CLI。

### 阶段 7：GUI 依赖检查

- PyQt6 不在当前 `.venv` 或 pip 缓存中；授权下载在 124 秒后超时。
- 未以其他框架替代既定 PyQt6 技术栈，待依赖可用后继续 GUI 实现。

### 阶段 7：GUI 实时进度

- **状态：** 进行中
- 已完成：
  - 后台扫描线程通过 Qt 信号转发既有 ScanProgress 快照，避免从工作线程直接操作界面。
  - 状态栏实时显示已完成/已发现文件数、已 Hash 数据量、吞吐速率、ETA 与当前路径；扫描结束或失败后自动隐藏指示器。
  - 新增 GUI 回归测试，覆盖状态栏更新与报告交给系统默认浏览器打开。
- 验证：
  - Ruff 格式与静态检查通过。
  - 完整单元测试 38 项通过；一次 Windows 临时目录访问冲突重跑后未复现。
- 下一步：实现扫描配置与排除规则界面，再接入比较流程、错误列表和任务恢复提示。

### 阶段 7：扫描配置加载

- 已在 GUI 中加入 TOML 配置选择入口；扫描线程使用其中的并发、读取块、SHA512 与排除规则快照。

### 阶段 7：任务恢复入口

- 已完成：GUI 可从任务列表选择 `PAUSED`、`CANCELLED` 或 `FAILED` 扫描，在独立后台线程中调用既有恢复服务。
- 验证：新增恢复线程参数传递的 GUI 回归测试；完整单元测试 40 项通过。
- 后续：增加错误列表、比较流程及比较结果筛选。
### 阶段 7：错误列表

- 已完成：GUI 可展示选中扫描任务持久化的路径、错误代码和错误说明。
- 验证：新增错误列表 GUI 回归测试；完整单元测试 41 项通过。一次扫描恢复用例的时序失败重跑后未复现。
- 后续：实现文件/目录比较与比较结果筛选。
### 阶段 7：快照比较入口

- 已完成：GUI 可多选两个已完成扫描，在后台线程调用快照比较服务并持久化比较任务。
- 验证：新增后台比较线程 GUI 回归测试；完整单元测试 42 项通过。
- 后续：展示比较任务的结果明细和筛选，并接入实时文件/目录比较。
### 阶段 7：比较结果筛选

- 已完成：GUI 可展示最近比较任务的持久化条目，并按 `MATCH`、`CHANGED`、`ADDED`、`MISSING`、`ERROR` 等状态筛选。
- 验证：新增比较结果筛选 GUI 回归测试；完整单元测试 43 项通过。
- 后续：接入实时文件/目录比较和比较报告的直接打开入口。
### 阶段 7：文件扫描入口

- 已完成：GUI 支持目录和单个文件作为扫描目标，并共用后台线程、进度、暂停、取消和配置快照路径。
- 验证：新增单文件扫描 GUI 回归测试；完整单元测试 44 项通过。
- 后续：接入实时文件/目录比较以及比较报告的直接打开入口。
### 阶段 7：实时目录比较

- 已完成：GUI 可选择一个已完成历史快照与当前目录，在后台扫描当前目录后生成可筛选的比较任务。
- 验证：新增实时目录比较 GUI 回归测试；完整单元测试 45 项通过。
- 阶段状态：核心 GUI 功能已实现，仍待本机人工验收完整窗口交互与关闭后恢复行为。
### 阶段 7：GUI 端到端扫描验证

- 已完成：自动化验证窗口可启动真实后台目录扫描，等待完成后读取到持久化的 `COMPLETED` 快照并刷新任务列表。
- 验证：完整单元测试 46 项通过。
- 保留：`file://` 报告的视觉人工验收仍受浏览器自动化安全策略限制。
### 阶段 7：GUI 端到端恢复验证

- 已完成：自动化验证窗口可恢复持久化的 `CANCELLED` 扫描，并在后台完成同一快照。
- 验证：完整单元测试 47 项通过。
### 阶段 8：Windows 本机构建

- 已完成：修正 `diskhtml-gui` 控制台入口声明，新增 PyInstaller 构建依赖、图形界面入口与 `scripts/build_windows.ps1` 构建脚本。
- 构建：本机已成功生成 `build\dist\DiskHTML\DiskHTML.exe`；构建产物由 `.gitignore` 排除。
- 验证：PyInstaller 构建成功；Ruff 格式检查与静态检查通过；完整单元测试 47 项通过。
- 待办：在干净 Windows 10/11 环境执行新建项目、扫描、恢复、比较、报告打开和重启恢复的人工验收。
### 阶段 8：项目数据自校验

- 已完成：新增只读的 `check-project` 命令，统一校验 SQLite 完整性、模式版本、迁移记录、任务引用关系、状态枚举和扫描进度计数。
- 验证：新增数据库与 CLI 回归测试；正常项目可通过校验，模拟孤立记录和计数异常会被明确报告。
- 保留：导出一致性已由扫描报告与比较报告的回归测试覆盖。
### 阶段 8：报告原子发布重试

- 已完成：比较报告改用与扫描报告相同的原子目录发布逻辑；遇到短暂的 Windows 文件锁会有限重试后发布。
- 验证：新增发布重试回归测试，并通过扫描报告、比较报告针对性测试。
### 阶段 8：可复现性能基准

- 已完成：新增 `scripts/benchmark_scan.py`，一次运行会保存 Hash 吞吐、峰值工作集、数据库大小、报告大小及两阶段耗时到 `result.json`。
- 介质建议：HDD 从 1 个 Hash 工作线程和 8 项队列起步；SSD 从 2 个工作线程和 32 项队列起步，均可通过脚本参数调整。
- 验证：两文件小样本已成功生成数据库、离线报告与完整指标；新增端到端回归测试覆盖该流程。
- 待办：仍需在目标 HDD、SSD 和 300 万文件压力数据集上执行正式测量并保存原始结果。
### 阶段 8：用户手册与发布清单

- 已完成：新增 GUI/CLI 用户操作手册，覆盖项目自检、扫描恢复、比较、离线报告和导入备份。
- 已完成：新增发布检查清单，涵盖质量检查、打包、HDD/SSD 基准证据、Windows 10/11 人工验收和发布归档。
- 保留：干净 Windows 10/11 的实际人工验收仍需在目标环境执行后逐项勾选。
### 阶段 8：权限错误审计

- 已完成：扫描器将文件和目录访问异常映射为稳定领域错误码；权限拒绝为 `PERMISSION_DENIED`，文件消失为 `FILE_DISAPPEARED`。
- 验证：新增模拟权限拒绝的端到端扫描恢复测试，确认任务完成、文件记录和错误列表均保留该错误码。
### 阶段 8：元数据与大文件可靠性覆盖

- 已完成：新增 1,000 个小文件的真实扫描测试，验证任务进度、文件索引和项目自校验的一致性。
- 已完成：新增 64 MiB 稀疏文件的分块 Hash 测试，验证大文件的摘要、字节进度和项目自校验。
- 保留：300 万文件压力模型仍需在正式硬件环境执行，不能由本机小样本替代。
### 阶段 8：压力数据集生成

- 已完成：新增 `scripts/generate_stress_dataset.py`，以可配置目录扇出创建新数据集并写入 `dataset.json`。
- 验证：小规模端到端测试确认文件数量、逻辑大小和清单一致；默认规模限制为 10,000 文件，300 万规模必须显式传参。
- 待办：在专用 HDD、SSD 上生成正式数据集并归档对应清单、基准结果和硬件信息。
### 阶段 8：基准结果完整性

- 已完成：性能基准在报告导出与数据库检查点后执行项目自校验；校验失败时不产生可用的 `result.json`。
- 验证：端到端测试确认结果包含 `validation.project_check = ok`。

### 阶段 8：本机 10,000 文件元数据基线

- 执行：在 `C:\tmp` 生成 10,000 个零字节文件并以 1 个工作线程、8 项队列完成扫描、报告导出和项目自校验。
- 结果：扫描 13.130888 秒，报告 0.411245 秒，数据库 6,168,576 字节，报告 9,278,111 字节，峰值工作集 34,861,056 字节。
- 限制：运行盘介质类型未确认，且数据集为零字节元数据集；Hash 吞吐仅 12.41 字节/秒，不可作为 HDD/SSD 或大文件 Hash 性能结论。
